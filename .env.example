# Copy to .env and fill in. All VITE_ variables are PUBLIC (bundled into the browser).
# Never put service-role keys or Resend keys here.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_APP_URL=http://localhost:5173
VITE_ENABLE_DEMO_MODE=true
